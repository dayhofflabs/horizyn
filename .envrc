#!/usr/bin/env bash
# direnv shim – sources the shared template maintained by StudioDC
if [[ -n "${WORKSPACE_ROOT:-}" ]]; then
    source "${WORKSPACE_ROOT}/.devcontainer/dotfiles/.envrc"
else
    # Fallback for users opening the repo outside StudioDC
    source "$HOME/.devcontainer/dotfiles/.envrc"
fi
